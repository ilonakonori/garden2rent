  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1516253593875-bd7ba052fbc5?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1500&q=80)";>
    <div class="container-banner text-center">
      <h1>Rent a <strong> garden </strong> in <strong> Berlin </strong> without the hassle</strong>!</h1>
      <h2 style="margin-bottom: 3rem;">You do not need to go far to find your perfect garden!</h2>
      <%= link_to "Book now", gardens_path(@gardens), class: "button button-text" %>
    </div>
  </div>

<div class="container" id="home">
  <div class='row justify-content-around card-container' style="padding: 40px 72px;">
    <% @gardens.each do |garden| %>
      <div class="card-trip" style="max-width: 300px;">
        <%= link_to garden_path(garden) do %>
          <%= cl_image_tag garden.photos.first.key, height: 200, width: 300, crop: :fill %>
        <% end %>
        <div class="card-trip-infos">
          <div>
            <h2><%= link_to garden.title, garden_path(garden) %></h2>
            <% garden.reviews.count == 1 ? sg_pl = "review" : sg_pl = "reviews" %>
            <p><%= garden.avg_rating %> (<%= garden.reviews.count %> <%= sg_pl %>)</p>
            <p>Price: €<%= garden.price %> / day</p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

