  <div class="banner">
    <div class="container-banner text-center">
      <h1>Rent a garden in Berlin without the hassle!</h1>
      <h2 style="margin-bottom: 3rem;">You do not need to go far to find your perfect garden!</h2>
      <%= link_to "Book now", gardens_path(@gardens), class: "button-text btn-banner" %>
    </div>
  </div>

<div class="container" id="home">
  <div class='row justify-content-around card-container' style="padding: 40px 72px;">
    <% @gardens.each do |garden| %>
      <div class="card-trip" style="max-width: 300px;">
        <%= link_to garden_path(garden) do %>
          <%= cl_image_tag garden.photos.first.key, height: 200, width: 300, crop: :fill %>
        <% end %>
        <div class="card-trip-infos">
          <div>
            <h2><%= link_to garden.title, garden_path(garden) %></h2>
            <p class="card-text"><%= garden.avg_rating %> (<%= pluralize garden.reviews.count, "review" %>)</p>
            <p>Price: €<%= garden.price %> / day</p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

